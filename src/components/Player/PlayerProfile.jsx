// TODO: Personal match history + head-to-head + season summary.
export default function PlayerProfile({ player }) {
  return (
    <div className="text-sm text-gray-500">
      PlayerProfile — TODO ({player?.name ?? "—"})
    </div>
  );
}
